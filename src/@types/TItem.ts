export interface IPostItem {
  name: string
  description: string
  daily_value: number
  max_days: number
  min_days: number
  categories: string[]
};
export interface IPostImage {
  imagePath: string[]
}